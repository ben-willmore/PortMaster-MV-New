name: Build Ship of Harkinian
on:
  # Build on request
  workflow_dispatch
#  schedule:
#    - cron:  '5 4 * * *'
jobs:
  build:
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            tools
            ports/soh
          sparse-checkout-cone-mode: false
      - name: Build port
        run: |
          . ./tools/build_port.txt ports/soh/soh
      - name: Get current date
        id: date
        run: echo "::set-output name=date::$(date +'%Y-%m-%d')"
      - name: Create git tag and upload
        run: |
          git config --global user.email "ben@willmore.eu"
          git config --global user.name "Github Action"
          git pull origin
          git add ports/soh
          git commit -m "SoH release build ${{ steps.date.outputs.date }}"
          git tag SoH-release-${{ steps.date.outputs.date }}
          git push origin
      - name: Publish packaged artifacts
        uses: actions/upload-artifact@v4
        with:
          name: SoH-Portmaster
          path: |
            ports/soh/Ship of Harkinian.sh
            ports/soh/soh
          retention-days: 7
          overwrite: true
          include-hidden-files: true